export interface AsyncInterface {
  // delayExecution(fn: () => void, ms: number): void
  //
  // repeatExecution(fn: () => void, ms: number): void
  //
  // delayCallback(obj: any): string[];
  //
  // delaySync(obj: any): string[];
  //
  // delayPromise(obj: any): string[];
}
